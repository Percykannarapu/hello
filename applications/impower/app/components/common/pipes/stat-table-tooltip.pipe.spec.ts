import { StatTableTooltipPipe } from './stat-table-tooltip.pipe';

describe('StatTableTooltipPipe', () => {
  it('create an instance', () => {
    const pipe = new StatTableTooltipPipe();
    expect(pipe).toBeTruthy();
  });
});
